database:
  host: "{{ hostvars[groups['db'][0]].private_ip }}"
  port: {{ db_port }}
  user: "{{ db_user }}"
  password: "{{ vault_db_password }}"
  database: "{{ db_name }}"

server:
  host: "0.0.0.0"
  port: {{ app_port }}
