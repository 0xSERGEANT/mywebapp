server {
    listen 80 default_server;
    server_name _;

    proxy_set_header Host            $host;
    proxy_set_header X-Real-IP       $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

    location ^~ /health {
        return 404;
    }

    location = / {
        proxy_pass http://app:5200;
    }

    location = /items {
        proxy_pass http://app:5200;
    }

    location ~ ^/items/[0-9]+$ {
        proxy_pass http://app:5200;
    }

    location / {
        return 404;
    }
}